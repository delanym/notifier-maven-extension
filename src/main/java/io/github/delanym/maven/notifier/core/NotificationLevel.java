package io.github.delanym.maven.notifier.core;

/**
 * Severity level for a build notification, used to influence display style
 * (color, urgency, icon) in each notification channel.
 */
public enum NotificationLevel {
    INFO,
    WARNING,
    ERROR
}
