/**
 * MQTT v5 notification channel that publishes build events as JSON messages
 * to a topic on a Mosquitto (or compatible) broker.
 */
@NullMarked
package io.github.delanym.maven.notifier.channel.mqtt;

import org.jspecify.annotations.NullMarked;
