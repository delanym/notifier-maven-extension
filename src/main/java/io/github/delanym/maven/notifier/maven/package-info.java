/**
 * Maven integration layer: the
 * {@link io.github.delanym.maven.notifier.maven.NotifierEventSpy} hooks into
 * Maven's lifecycle via JSR-330 / Sisu discovery and notifyes build
 * outcome notifications through the configured channel(s).
 */
@NullMarked
package io.github.delanym.maven.notifier.maven;

import org.jspecify.annotations.NullMarked;
