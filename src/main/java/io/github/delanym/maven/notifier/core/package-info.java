/**
 * Core abstractions for the notifier notification system: the
 * {@link io.github.delanym.maven.notifier.core.Notifier} contract, the
 * {@link io.github.delanym.maven.notifier.core.Notification} payload, and
 * shared utilities (icon handling, command execution, HTTP base class)
 * used by all notifier implementations.
 */
@NullMarked
package io.github.delanym.maven.notifier.core;

import org.jspecify.annotations.NullMarked;
